import {Component, Directive, ElementRef, Input, OnInit, Renderer2} from '@angular/core';
import {DomController, GestureConfig, GestureController, Platform} from '@ionic/angular';

@Component({
  selector: 'app-content-drawer',
  templateUrl: './content-drawer.component.html',
  styleUrls: ['./content-drawer.component.scss'],
})
export class ContentDrawerComponent implements OnInit {

  ngOnInit() {
  }

}
